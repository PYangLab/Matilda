% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tasks.R
\name{matilda_task_files}
\alias{matilda_task_files}
\title{Run Matilda tasks from file paths (mirrors main_matilda_task.py / main_matilda_rna_task.py); writes to outdir.}
\usage{
matilda_task_files(
  model,
  rna,
  adt = NULL,
  atac = NULL,
  cty,
  classification = FALSE,
  fs = FALSE,
  dim_reduce = FALSE,
  simulation = FALSE,
  query = FALSE,
  fs_method = "IntegratedGradient",
  simulation_ct = -1,
  simulation_num = 100L,
  outdir = ".",
  device = c("auto", "cpu", "cuda")
)
}
\arguments{
\item{model}{a \code{matilda_model}.}

\item{rna, adt, atac, cty}{input file paths.}

\item{classification, fs, dim_reduce, simulation, query}{task flags.}

\item{fs_method, simulation_ct, simulation_num}{task options.}

\item{outdir}{directory to copy results into.}

\item{device}{"auto"/"cpu"/"cuda".}
}
\value{
the output directory, invisibly.
}
\description{
Power-user / validation entry point: drives the unchanged task script on raw
.h5/.csv inputs and copies the produced \code{output/} tree to \code{outdir}.
}
\examples{
\donttest{
  f <- matilda_example_teaseq()
  m <- matilda_train_files(f["train_rna"], f["train_adt"], f["train_atac"],
                           f["train_cty"], epochs = 2L)
  matilda_task_files(m, f["test_rna"], f["test_adt"], f["test_atac"],
                     f["test_cty"], classification = TRUE, query = TRUE,
                     outdir = tempfile())
}
}
