% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/train.R
\name{matilda_train_files}
\alias{matilda_train_files}
\title{Train Matilda from .h5 / .csv file paths (mirrors main_matilda_train.py / main_matilda_rna_train.py).}
\usage{
matilda_train_files(
  rna,
  adt = NULL,
  atac = NULL,
  cty,
  batch_size = 64L,
  epochs = 30L,
  lr = 0.02,
  z_dim = 100L,
  hidden_rna = 185L,
  hidden_adt = 30L,
  hidden_atac = 185L,
  augmentation = TRUE,
  seed = 1L,
  device = c("auto", "cpu", "cuda")
)
}
\arguments{
\item{rna, adt, atac}{paths to per-modality .h5 files (adt/atac may be NULL).}

\item{cty}{path to the cell-type label .csv.}

\item{batch_size, epochs, lr, z_dim, hidden_rna, hidden_adt, hidden_atac, augmentation, seed}{training hyperparameters (defaults match upstream Matilda).}

\item{device}{one of "auto", "cpu", "cuda".}
}
\value{
a \code{matilda_model}.
}
\description{
Train Matilda from .h5 / .csv file paths (mirrors main_matilda_train.py / main_matilda_rna_train.py).
}
\examples{
defaults <- formals(matilda_train_files)[c("epochs", "lr", "z_dim")]
\donttest{
  f <- matilda_example_teaseq()
  m <- matilda_train_files(f["train_rna"], f["train_adt"], f["train_atac"],
                           f["train_cty"], epochs = 2L)
}
}
