% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/train.R
\name{matilda_train}
\alias{matilda_train}
\title{Train Matilda on single-cell multimodal data.}
\usage{
matilda_train(
  x = NULL,
  label = NULL,
  rna = NULL,
  adt = NULL,
  atac = NULL,
  cty = NULL,
  assay = "counts",
  adt_exp = "ADT",
  atac_exp = "ATAC",
  batch_size = 64L,
  epochs = 30L,
  lr = 0.02,
  z_dim = 100L,
  hidden_rna = 185L,
  hidden_adt = 30L,
  hidden_atac = 185L,
  augmentation = TRUE,
  seed = 1L,
  device = c("auto", "cpu", "cuda")
)
}
\arguments{
\item{x}{an SCE/MAE/Seurat object, or \code{NULL} to use the matrix arguments.}

\item{label}{colData column name (or a vector) of cell types (object input).}

\item{rna, adt, atac}{genes x cells matrices (used only when \code{x} is \code{NULL}).}

\item{cty}{cell-type labels vector (used only when \code{x} is \code{NULL}).}

\item{assay, adt_exp, atac_exp}{assay / altExp selectors for SCE/MAE input.}

\item{batch_size, epochs, lr, z_dim, hidden_rna, hidden_adt, hidden_atac, augmentation, seed}{training hyperparameters (defaults match upstream Matilda).}

\item{device}{one of "auto", "cpu", "cuda".}
}
\value{
the input object with the model stored, or a \code{matilda_model} for matrices.
}
\description{
Trains the multimodal VAE + classifier. For a
\link[SingleCellExperiment]{SingleCellExperiment} or
\link[MultiAssayExperiment]{MultiAssayExperiment} the trained model is stored
inside the object (\code{metadata(x)$matilda}) and the object is returned, so
it pipes straight into \code{\link{matilda_reduce}} / \code{\link{matilda_classify}}.
For plain matrices it returns a \code{matilda_model}.
}
\examples{
sce <- matilda_example_sce()
\donttest{
  sce <- matilda_train(sce, label = "cell_type", epochs = 2L)
}
}
